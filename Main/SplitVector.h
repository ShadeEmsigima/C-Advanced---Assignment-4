#pragma once
#include<vector>
#include <string>
#include <algorithm> // to use partioning operators

// Met kopie van de vectors de vector in 2 nieuwe vectoren te splitsen, 
//1 met alles wat alfabetisch voor 'purple' komt, 1 met alles er na




/*

- for the first letter of every item in the passed vector
- Check if that letter comes before "p" in the alphabet
- store it in the one vector if it is, store it in another if its not

*/


























//if (first letter of item == for every item in alphabetBefPurp)
//{put it in itemsBefPurp};
//else {put it in itemsAftPurp};
std::vector<std::string> itemsBefPurp {};
std::vector<std::string> itemsAftPurp {};
std::vector<std::string> alphabetBefPurp {"b", "g", "o", "p"};
